#include <stdio.h>
#include <math.h>
int main()
{
    int num[101], test, squrtNumber, i, j;
    scanf("%d", &test);
    for (i = 0; i < test; i++)
    {
        scanf("%d", &num[i]);
       
        
    }


        for (j = 0; j <= ; j++)
        {  

        if (num[j-3] == 1 ){
    printf("No\n");
        }
           

       if (num[j-3] == 2){
  printf("Yes\n");
        }
          

        // squrtNumber = sqrt(num[i]); 

            if (num[j-3] % j == 0)
            {
              
                 printf("No\n");
            }
            else
            {
                 printf("Yes\n");
            }
        }
    
    return 0;
}