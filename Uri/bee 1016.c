#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int distance, minutes;
    scanf("%d", &distance);
    minutes = distance * 2;
    printf("%d minutos\n", minutes);

    return 0;
}